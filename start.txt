to start the site run the following commands in bash:

python3 -m venv auth
source auth/bin/activate
pip install -r requirements.txt
flask run

##To run unit test:
python3 -m test
coverage run -m test
coverage report -m


##To run selenium test:
python3 -m systemtests